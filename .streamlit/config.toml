[general]
email = ""

[server]
# 设置会话无操作超时时间（单位：秒）
# 默认是无限制，这里设置为1小时（3600秒）
# 用户分析完成后1小时内页面不会自动刷新
enableXsrfProtection = false
maxUploadSize = 200

[runner]
# 脚本运行后的超时时间（单位：秒）
# 设置为0表示永不超时
# 如果不想永不超时，可以设置为3600（1小时）或更长
fastReruns = true
